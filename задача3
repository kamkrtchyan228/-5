def convert_usd_to_rub(amount_usd):
    """
    Конвертирует сумму из долларов США в российские рубли.
    
    Args:
        amount_usd (float): Сумма в долларах США для конвертации
        
    Returns:
        float: Сумма в российских рублях после конвертации
    """
    USD_TO_RUB = 95.50
    return amount_usd * USD_TO_RUB


def main():
    """
    Основная функция программы, которая запрашивает у пользователя данные,
    выполняет конвертацию и выводит результат.
    """
    try:
        # Запрос суммы у пользователя
        usd_amount = float(input("Введите сумму в долларах США: "))
        
        # Проверка на неотрицательное значение
        if usd_amount < 0:
            print("Ошибка: сумма не может быть отрицательной.")
            return
        
        # Конвертация в рубли
        rub_amount = convert_usd_to_rub(usd_amount)
        
        # Вывод результата
        print(f"{usd_amount:.2f} USD = {rub_amount:.2f} RUB")
        print(f"Курс: 1 USD = 95.50 RUB")
        
    except ValueError:
        print("Ошибка: пожалуйста, введите числовое значение.")


# Запуск программы
if __name__ == "__main__":
    main()
